# CI / test environment — override via environment variables in the pipeline
BASE_URL=http://localhost:5173
API_URL=http://localhost:8080/RipserApp

# Injected by CI secrets — never hardcode here
TEST_USERNAME=${TEST_USERNAME}
TEST_PASSWORD=${TEST_PASSWORD}

TEST_EMPRESA_ID=1
TEST_SUCURSAL_ID=1
