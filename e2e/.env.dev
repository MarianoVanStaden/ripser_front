# Local development environment
BASE_URL=http://localhost:5173/ripser/
# ⚠️ SIEMPRE localhost acá. Este archivo apuntó al VPS de prod y la suite
# estuvo sembrando clientes/productos/documentos EN PRODUCCIÓN (sep-2026).
# Si necesitás correr contra otro entorno, usá TEST_ENV=<env> con su .env propio.
API_URL=http://localhost:8080/RipserApp

# Default test credentials (dev only — do NOT use real passwords in .env.test)
TEST_USERNAME=adminempresa
TEST_PASSWORD=000

# Tenant context
TEST_EMPRESA_ID=1
TEST_SUCURSAL_ID=1
